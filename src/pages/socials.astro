---
import Layout from "../layouts/Layout.astro";
import { SOCIAL_ACCOUNTS } from "../config.ts";
---

<Layout title="socials">
<h1 class="text-4xl">socials</h1>

hello, you can basically find me everywhere with the username @thatgurkangurk :)

{SOCIAL_ACCOUNTS.map((account) => {
    if (account.link) return <div><a href={account.link}>{account.platform}</a></div>

    return <div>{account.platform}: {account.username}</div>
}
)}

</Layout>
